{
  "name": "assistant",
  "private": true,
  "description": "Agent home repo — identity, skills, and tooling",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
